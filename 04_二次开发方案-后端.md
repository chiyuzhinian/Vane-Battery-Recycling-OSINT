# ⚙️ 二次开发方案 - 后端设计

> **前置阅读**：[03_数据源管理和验证体系.md](03_数据源管理和验证体系.md)
> **本文定位**：把 03 的"信息源 + 验证规则"落地成可运行的后端系统，明确模块边界、数据流、调度策略和 API 契约。
> **不包含**：抓取脚本逐行实现见 [07_Python采集脚本详解.md](07_Python采集脚本详解.md)；Claude 分析详见 [08_Claude集成和深度分析.md](08_Claude集成和深度分析.md)。

---

## 第一部分：后端架构总览

### 1.1 需求 → 设计映射

下表是把你的原始需求逐条翻译成后端设计决策，**任何一条都不能在实现时丢**：

| # | 你的需求 | 后端设计决策 | 章节 |
|---|---|---|---|
| 1 | "希望 Vane 在**需求拆解**和**数据源搜证**环节做到最好" | 双引擎：`QueryDecomposer` + `EvidenceCollector`，拆解结果落库可追溯 | 2.1 / 2.2 |
| 2 | "形成采集领域**稳定可靠的信息源**" | 数据源注册中心（Source Registry），一源一档，权重/可信度/健康度全量记录 | 2.3 |
| 3 | "可以**动态更新数据源**以及人工手动添加稳定准确信息源" | 自学习引擎自动调权 + 白名单人工锁定，两套机制互不覆盖 | 第三部分 |
| 4 | "自己能够根据后端交付的数据来**发现数据源规律**，自己**纠错**" | `SourceScoreModel` 评分 + `PatternMiner` 规律发现 + 自动纠错动作（降权/禁用/候选新增） | 3.1 / 3.2 / 3.3 |
| 5 | "**Claude 来做深度分析**" | 后端只做**薄适配层**（异步任务 + 结构化出入参），Prompt 与模板全在 08 篇 | 2.7 |
| 6 | "**交叉验证**保证数据质量，验证环节可以利用 AI 或人工审核" | 五层验证流水线（与 03 决策树 1:1 对应）+ 待审队列 | 2.4 / 2.8 |
| 7 | "数据的存储使用**云端存储**" | 云服务器自建：MySQL 8 + 对象存储（MinIO/OSS），Docker Compose 一键起 | 1.4 |
| 8 | "**人工审核只有我**" | 单人审核工作台：不设角色流转，设"优先级 + 批量操作 + 键盘流" | 2.8 |
| 9 | 政策监测**每周**，企业信息**每天**希望有更新 | 双调度周期：企业增量每日、政策全量每周，见 4.2 | 4.2 |
| 10 | "先**欧盟和美国**" | 海外源分 `EU` / `US` 两个分组，独立开关、独立可信度基线 | 2.3 |
| 11 | "**先对格林美和邦普完善**" | 企业种子清单先落 2 家，但维度模板按 20-30 家设计（不写死） | 2.1 |
| 12 | 告警走 **钉钉** | 钉钉自定义机器人 Webhook + 卡片消息 | 6.2 |
| 13 | "需要**保留对话检索入口**" | `/api/chat/search` 代理透传 Vane，与后台批量采集共用同一套验证/入库管线 | 2.9 |
| 14 | "**数据最重要**，不希望检索出来的数据最后是错误展示在上面" | 质量闸门前置：**未过闸不落正式表**，前端永远读不到未验证脏数据 | 6.1 |

### 1.2 分层架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          接入层 (FastAPI)                                │
│  REST API  │  SSE 进度推送  │  对话检索代理  │  钉钉回调 / 告警出口        │
├─────────────────────────────────────────────────────────────────────────┤
│                          编排层 (Orchestrator)                           │
│  TaskScheduler(Celery Beat)  │  PipelineRunner  │  RetryPolicy           │
│  ─ 任务定义: 企业增量 / 政策周更 / 人工投喂 / 深度分析 / 源自学习          │
├─────────────────────────────────────────────────────────────────────────┤
│                          能力层 (Engines)                                │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐   │
│  │Query         │ │Evidence      │ │Cross         │ │Entity        │   │
│  │Decomposer    │→│Collector     │→│Validator     │→│Resolver      │   │
│  │需求拆解      │ │多源搜证      │ │交叉验证      │ │实体消解/去重 │   │
│  └──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘   │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐   │
│  │Source        │ │Pattern       │ │Claude        │ │Review        │   │
│  │Registry      │ │Miner         │ │Adapter       │ │Service       │   │
│  │数据源注册    │ │规律发现/纠错 │ │深度分析适配  │ │人工审核      │   │
│  └──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘   │
├─────────────────────────────────────────────────────────────────────────┤
│                          数据层 (Storage)                                │
│  MySQL 8  │  Redis(Celery+缓存)  │  对象存储(原始快照/报告)  │  向量库(Faiss)│
├─────────────────────────────────────────────────────────────────────────┤
│                          外部依赖 (External)                             │
│  Vane /api/search  │  Claude API  │  定向源直采  │  钉钉机器人            │
└─────────────────────────────────────────────────────────────────────────┘
```

**关键数据流（一条情报的完整生命周期）**：

```
需求拆解 → 生成查询矩阵 → 多源搜证 → 原始快照落对象存储
    → 结构化抽取 → 断言(Claim)聚类 → 交叉验证打分
    → 质量闸门 ┬─ ≥80分 ──→ 直接入正式表 (verified)
               ├─ 60-79分 ─→ 待审队列 → 你审核 → 入正式表 (reviewed)
               └─ <60分 ───→ 强制人工审核 → 通过/拒绝
    → 源效果回流 → 自学习调权 → 影响下一轮采集策略
```

### 1.3 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| Web 框架 | **FastAPI** | 异步原生，SSE/WebSocket 友好，自动 OpenAPI 文档 |
| 任务队列 | **Celery + Redis** | 采集/分析都是长任务，必须异步；Beat 做周期调度 |
| 数据库 | **MySQL 8.0** | 与 06 篇 Schema 对应；JSON 字段存半结构化维度数据 |
| ORM | **SQLAlchemy 2.0 + Alembic** | 迁移可控 |
| HTTP | **httpx (async)** | 并发搜证 |
| 抓取解析 | **BeautifulSoup4 + lxml + Playwright（按需）** | 静态优先，动态页面兜底 |
| 文档解析 | **pdfplumber + python-docx** | 年报/环评 PDF 提取 |
| 向量 | **sentence-transformers + Faiss** | 语义去重与交叉验证的相似度计算 |
| 分析 | **anthropic SDK** | Claude 深度分析 |
| 校验 | **pydantic v2** | 抽取结果强类型校验 |
| 告警 | **钉钉机器人 Webhook** | 你的指定渠道 |
| 部署 | **Docker Compose** | 云服务器自建，一台机器可跑全栈 |

> **为什么不用云托管数据库/OSS？** 你确认了用**普通云服务器**。全部组件容器化后 `docker compose up -d` 一键拉起，迁移成本最低、无厂商绑定；等数据量上来再把 MySQL 换成云 RDS、对象存储换成云 OSS（配置项已抽到 `.env`，改环境变量即可）。

### 1.4 部署拓扑

```yaml
# docker-compose.yml
version: "3.9"

services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: battery_osint
      TZ: Asia/Shanghai
    command: >
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
      --innodb-buffer-pool-size=1G
    volumes: [ "mysql-data:/var/lib/mysql" ]
    ports: [ "127.0.0.1:3306:3306" ]   # 只对本机开放

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
    volumes: [ "redis-data:/data" ]

  minio:                                # 对象存储：原始快照 + 生成的报告
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_PASSWORD}
    volumes: [ "minio-data:/data" ]
    ports: [ "127.0.0.1:9000:9000", "127.0.0.1:9001:9001" ]

  vane:                                 # 采集底座，独立升级
    image: itzcrazykns1337/vane:latest
    environment:
      SEARXNG_API_URL: ${SEARXNG_API_URL}
    volumes: [ "vane-data:/home/vane/data" ]
    ports: [ "127.0.0.1:3000:3000" ]

  api:                                  # FastAPI
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    env_file: [ .env ]
    depends_on: [ mysql, redis, minio ]
    ports: [ "127.0.0.1:8000:8000" ]

  worker:                               # Celery worker
    build: .
    command: celery -A app.tasks worker -l info -Q collect,analyze,maintain -c 4
    env_file: [ .env ]
    depends_on: [ mysql, redis ]

  beat:                                 # Celery Beat 周期调度
    build: .
    command: celery -A app.tasks beat -l info
    env_file: [ .env ]
    depends_on: [ redis ]

  nginx:                                # TLS 终止 + 反向代理
    image: nginx:alpine
    volumes: [ "./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro" ]
    ports: [ "80:80", "443:443" ]
    depends_on: [ api ]

volumes:
  mysql-data:
  redis-data:
  minio-data:
  vane-data:
```

> **安全基线**：除 Nginx 外所有端口都绑 `127.0.0.1`，外部只能通过 443 访问；Nginx 上加 Basic Auth 或 Token，因为这套系统里存的是你的研究底稿。

### 1.5 目录结构

```
app/
├── main.py                     # FastAPI 入口
├── config.py                   # pydantic-settings 读 .env
├── api/
│   ├── deps.py                 # 依赖注入（DB session、鉴权）
│   └── v1/
│       ├── collect.py          # 采集任务：创建/查询/取消
│       ├── evidence.py         # 证据/断言：列表/详情/溯源
│       ├── companies.py        # 企业档案
│       ├── policies.py         # 政策库
│       ├── review.py           # 人工审核
│       ├── sources.py          # 数据源管理（人工增删/锁定）
│       ├── chat.py             # 对话检索代理（保留入口）
│       └── analytics.py        # 统计/源自学习报表
├── engines/
│   ├── decomposer.py           # 需求拆解
│   ├── collector.py            # 多源搜证（三通道）
│   ├── validator.py            # 交叉验证（五层）
│   ├── resolver.py             # 实体消解与去重
│   ├── extractor.py            # 5 维度结构化抽取
│   ├── claude_adapter.py       # Claude 适配层（薄）
│   ├── source_score.py         # 源效果评分
│   └── pattern_miner.py        # 规律发现 + 自动纠错
├── connectors/                 # 定向源连接器（一源一文件）
│   ├── base.py                 # BaseConnector 抽象
│   ├── cninfo.py               # 巨潮资讯（公告/年报）
│   ├── eia.py                  # 环评公示
│   ├── bidding.py              # 招投标
│   ├── patent.py               # 专利
│   ├── eu_lex.py               # 欧盟法规（EUR-Lex）
│   └── us_federal.py           # 美国联邦公报 / DOE / EPA
├── models/                     # SQLAlchemy 模型（对应 06 篇）
├── schemas/                    # Pydantic 出入参
├── tasks/
│   ├── celery_app.py
│   ├── collect_tasks.py
│   ├── analyze_tasks.py
│   └── maintain_tasks.py
└── core/
    ├── quality_gate.py         # 质量闸门
    ├── alerts.py               # 钉钉告警
    ├── storage.py              # 对象存储客户端
    └── logging.py

sources/
├── battery-recycling-sources.yaml   # 总入口：加载顺序、维度↔源映射、全局规则
├── policy-cn.yaml                   # 中国政策源（⏳ 待回填 URL）
├── policy-eu.yaml                   # 欧盟政策源
├── policy-us.yaml                   # 美国政策源
├── companies.yaml                   # 目标企业注册表（一文件一清单，含别名/代码/母子关系）
└── info-sources.yaml                # 行业信息源（咨询机构 + 协会组织）
```

> **设计说明**：企业配置采用**单文件注册表**（`companies.yaml`）而非一司一文件——因为企业会持续增加到 20-30 家，
> 单文件便于批量对比、统一校验和一次性 diff 审核。若某家企业需要独立的复杂采集逻辑，
> 可在 `sources/companies/<id>.override.yaml` 中单独覆盖（加载器优先读 override）。

---

## 第二部分：核心模块设计

### 2.1 需求拆解模块（Query Decomposer）

**职责**：把"我要格林美的人力资源情况"这种模糊需求，拆成**可执行、可验证、可去重**的查询矩阵。

拆解公式：

```
研究目标 = 企业(Entity) × 维度(Dimension) × 信息源类别(SourceType) × 时间窗(Period)
查询矩阵 Q = { q(e, d, s, t) | e ∈ 企业, d ∈ 5维度, s ∈ 源类别, t ∈ 时间窗 }
```

**5 维度字段清单（与你的需求逐条对应）**：

```python
# app/engines/decomposer.py
from dataclasses import dataclass, field
from datetime import date
from typing import Literal

Dimension = Literal["hr", "feedstock", "operation", "strategy", "technology"]

@dataclass(frozen=True)
class DimensionSpec:
    """维度定义：字段 + 优先源 + 抽取提示"""
    key: Dimension
    name: str
    fields: list[str]
    priority_sources: list[str]      # 源类别 id，见 03 篇 1.1
    query_templates: list[str]       # {company} {year} 占位

DIMENSIONS: dict[Dimension, DimensionSpec] = {
    "hr": DimensionSpec(
        key="hr",
        name="人力资源",
        fields=[
            "core_executives",        # 董事长/CEO/总裁 姓名与背景
            "management_team",        # 核心管理层（≥5人）
            "org_changes",            # 组织架构变动、任免公告
            "key_technical_talents",  # 首席科学家/技术总监
            "equity_incentive",       # 股权激励方案
            "headcount_change",       # 员工规模变化（招聘/裁员）
        ],
        priority_sources=["announcement", "annual_report", "news"],
        query_templates=[
            "{company} 高管 团队 背景 {year}",
            "{company} 董事 任免 公告 {year}",
            "{company} 股权激励 方案",
            "{company} 招聘 员工规模 {year}",
        ],
    ),
    "feedstock": DimensionSpec(
        key="feedstock",
        name="废料量及来源",
        fields=[
            "total_recycled_volume",  # 年回收量 / 处理能力
            "feedstock_sources",      # 主要废料来源（车企/电池厂）
            "material_breakdown",     # 三元/铁锂/磷酸铁锂 细分
            "channel_analysis",       # 直接回收 vs 梯次利用
            "regional_distribution",  # 地区来源分布
        ],
        priority_sources=["annual_report", "eia", "news"],
        query_templates=[
            "{company} 电池回收 处理能力 万吨 {year}",
            "{company} 废料 来源 渠道 合作",
            "{company} 梯次利用 直接回收 比例",
        ],
    ),
    "operation": DimensionSpec(
        key="operation",
        name="经营现状",
        fields=[
            "designed_capacity",      # 各基地设计产能
            "capacity_utilization",   # 产能利用率
            "output_volume",          # 回收产量（年/季）
            "revenue_profit",         # 电池回收业务营收/利润
            "gross_margin",           # 毛利率 / 净利率
            "cost_structure",         # 成本结构
            "cashflow_investment",    # 现金流 / 资本开支 / 融资
        ],
        priority_sources=["annual_report", "announcement", "industry_db"],
        query_templates=[
            "{company} {year} 年报 电池回收 营收",
            "{company} 产能 利用率 {year}",
            "{company} 毛利 成本 招股书",
        ],
    ),
    "strategy": DimensionSpec(
        key="strategy",
        name="战略规划",
        fields=[
            "new_projects",           # 新建项目/产线公告
            "geo_expansion",          # 区域扩张
            "overseas_layout",        # 海外布局、合作
            "strategic_alliances",    # 与车企/电池厂协议
            "ma_restructuring",       # 并购重组
            "channel_building",       # 渠道网络建设
            "value_chain_extension",  # 产业链上下游延伸
        ],
        priority_sources=["announcement", "news", "bidding"],
        query_templates=[
            "{company} 新建 项目 投资 公告 {year}",
            "{company} 海外 基地 合作",
            "{company} 战略合作 协议 车企",
        ],
    ),
    "technology": DimensionSpec(
        key="technology",
        name="工艺技术",
        fields=[
            "patents",                # 专利数量（授权/申请）
            "process_route",          # 湿法/火法/直接回收
            "recovery_rate",          # 锂/镍/钴 回收率
            "key_equipment",          # 自研设备
            "unit_cost",              # 单位吨位成本
            "innovation_trend",       # 技术创新动向
            "academic_partners",      # 高校/院所合作
        ],
        priority_sources=["patent", "annual_report", "news"],
        query_templates=[
            "{company} 专利 回收率 锂 镍 钴",
            "{company} 湿法 火法 工艺 技术路线",
            "{company} 技术 合作 高校 研究院",
        ],
    ),
}
```

**拆解器实现**：

```python
# app/engines/decomposer.py（续）
import itertools, hashlib

@dataclass
class QueryTask:
    query_id: str
    company: str
    dimension: Dimension
    source_type: str
    query: str
    period: str
    priority: int = 5           # 1=最高
    parent_goal: str | None = None

    def fingerprint(self) -> str:
        raw = f"{self.company}|{self.dimension}|{self.source_type}|{self.query}"
        return hashlib.sha1(raw.encode()).hexdigest()[:16]


class QueryDecomposer:
    def __init__(self, registry, claude=None):
        self.registry = registry          # SourceRegistry，见 2.3
        self.claude = claude              # 可选：用 Claude 做长尾改写

    def decompose(self, goal: str, company: str, dimensions: list[Dimension],
                  period: str, mode: str = "balanced") -> list[QueryTask]:
        """
        goal: 原始研究目标（原话），用于溯源
        mode: fast(少而精) / balanced / quality(广而深，会补长尾模板)
        """
        tasks: list[QueryTask] = []
        for dim in dimensions:
            spec = DIMENSIONS[dim]
            enabled = self.registry.enabled_sources()
            for src in spec.priority_sources:
                if src not in enabled:
                    continue
                for tpl in spec.query_templates:
                    q = tpl.format(company=company, year=period)
                    tasks.append(QueryTask(
                        query_id=f"{hashlib.sha1(q.encode()).hexdigest()[:12]}",
                        company=company, dimension=dim,
                        source_type=src, query=q, period=period,
                        priority=1 if src in ("announcement", "annual_report") else 3,
                        parent_goal=goal,
                    ))

        if mode == "quality" and self.claude:
            tasks += self._expand_with_llm(goal, company, dimensions, period)

        # 去重 + 排序（权威源优先、同源模板合并）
        seen, unique = set(), []
        for t in sorted(tasks, key=lambda x: x.priority):
            fp = t.fingerprint()
            if fp not in seen:
                seen.add(fp); unique.append(t)
        return unique

    def _expand_with_llm(self, goal, company, dimensions, period) -> list[QueryTask]:
        """质量模式下用 Claude 补充长尾查询（行业黑话、别名、地域变体）"""
        from app.engines.claude_adapter import ClaudeAdapter
        adapter = ClaudeAdapter()
        return adapter.suggest_queries(goal, company, list(dimensions), period)
```

**企业种子配置（先格林美、邦普，结构可扩展到 30 家）**：

```yaml
# sources/companies/gemm.yaml  —— 格林美
company:
  id: gemm
  name: 格林美
  full_name: 格林美股份有限公司
  aliases: [GEM, 格林美股份, 002340, 荆门格林美, 武汉格林美]
  stock_code: "002340.SZ"
  exchange: SZSE
  group: 电池回收
  enabled: true

  # 关键实体（提升实体消解准确率）
  subsidiaries: [荆门格林美, 武汉格林美, 无锡格林美, 江西格林美]
  key_people: []            # 采集后自动回填

  dimensions:
    hr:         { enabled: true, frequency: weekly }
    feedstock:  { enabled: true, frequency: weekly }
    operation:  { enabled: true, frequency: weekly }
    strategy:   { enabled: true, frequency: daily }   # 公告变动最频繁
    technology: { enabled: true, frequency: weekly }

  direct_sources:           # 定向直采（不依赖通用搜索）
    - type: cninfo
      params: { stock_code: "002340", category: [annual_report, announcement] }
    - type: eia
      params: { keyword: ["格林美", "格林美（荆门）", "荆门市格林美新材料"] }
    - type: patent
      params: { applicant: ["格林美股份有限公司", "荆门市格林美新材料有限公司"] }
```

```yaml
# sources/companies/brunp.yaml  —— 邦普（广东邦普循环科技，宁德时代子公司）
company:
  id: brunp
  name: 邦普
  full_name: 广东邦普循环科技有限公司
  aliases: [Brunp, 邦普循环, 湖南邦普, 宁德邦普, CATL邦普]
  stock_code: null          # 未独立上市，走母公司宁德时代公告穿透
  parent: { name: 宁德时代, stock_code: "300750.SZ" }
  group: 电池回收
  enabled: true
  note: |
    未独立上市 → 公告信息需从母公司宁德时代(300750)公告中检索关键词"邦普"；
    环评走湖南/广东/宁德地方生态环境局。
  dimensions:
    hr:         { enabled: true, frequency: weekly }
    feedstock:  { enabled: true, frequency: weekly }
    operation:  { enabled: true, frequency: weekly }
    strategy:   { enabled: true, frequency: daily }
    technology: { enabled: true, frequency: weekly }
  direct_sources:
    - type: cninfo
      params: { stock_code: "300750", keyword: ["邦普", "邦普循环"] }
    - type: eia
      params: { keyword: ["湖南邦普", "广东邦普", "宁德邦普"] }
```

> **为什么先只做两家？** 两家就能覆盖三种典型情况——**独立上市（格林美）**、**母公司穿透（邦普）**、**强专利属性**。把这两家的抽取模板、别名库、实体消解规则打磨到"零人工修正"，再复制到 20-30 家只是改 YAML，不是改代码。

### 2.2 多源搜证模块（Evidence Collector）

**三条采集通道**（缺一不可）：

```
┌──────────────────────────────────────────────────────────────────────┐
│                        Evidence Collector                            │
├──────────────────────────────────────────────────────────────────────┤
│ 通道 A：通用搜索（Vane）                                              │
│   用途：新闻、行业媒体、论坛、学术、长尾                                │
│   入口：POST {VANE_URL}/api/search                                    │
│   特点：多源聚合 + LLM 初筛，覆盖未知源                                 │
├──────────────────────────────────────────────────────────────────────┤
│ 通道 B：定向源直采（Connector）                                        │
│   用途：公告、年报、环评、招投标、专利、EUR-Lex、Federal Register       │
│   特点：结构化程度高、可信度高、可精确解析，是"稳定可靠信息源"的主体      │
├──────────────────────────────────────────────────────────────────────┤
│ 通道 C：人工投喂（Manual Feed）                                        │
│   用途：你手上已有的 PDF/截图/链接/内部资料                            │
│   特点：直接标为 high trust，跳过部分验证但仍入档可溯源                  │
└──────────────────────────────────────────────────────────────────────┘
```

**统一证据模型**：无论走哪条通道，最终都归一化成 `RawEvidence`：

```python
# app/engines/collector.py
from datetime import datetime
from pydantic import BaseModel, HttpUrl, Field

class RawEvidence(BaseModel):
    evidence_id: str
    channel: Literal["vane", "connector", "manual"]
    source_id: str                     # 对应 Source Registry 的源 id
    source_url: str | None
    source_title: str | None
    publish_date: datetime | None
    fetched_at: datetime = Field(default_factory=datetime.utcnow)
    raw_text: str                      # 清洗后正文
    raw_snapshot_key: str | None       # 对象存储 key（原始快照，永不删）
    query_id: str | None               # 由哪个查询任务产生
    company_hint: str | None
    dimension_hint: str | None
    content_hash: str                  # 用于粗去重


class EvidenceCollector:
    def __init__(self, vane, registry, storage, connectors):
        self.vane, self.registry = vane, registry
        self.storage, self.connectors = storage, connectors

    async def collect(self, task: QueryTask) -> list[RawEvidence]:
        src = self.registry.get(task.source_type)
        results: list[RawEvidence] = []

        # 通道 B 优先：有定向连接器就直接采，质量最高
        if src and src.connector and src.connector in self.connectors:
            results += await self.connectors[src.connector].fetch(task)
        # 通道 A 兜底：通用搜索补充
        if src is None or src.allow_general_search:
            results += await self._via_vane(task)

        return [self._persist_snapshot(e) for e in results if e.raw_text.strip()]

    async def _via_vane(self, task: QueryTask) -> list[RawEvidence]:
        """
        调用 Vane /api/search。参数与 Vane 前端一致：
          mode: fast | balanced | quality
          sources: web | academic | discussions
        """
        payload = {
            "query": task.query,
            "chatHistory": [],
            "mode": self.registry.mode_for(task),
            "sources": self.registry.vane_sources_for(task),
            "stream": False,
        }
        data = await self.vane.search(payload)
        out = []
        for item in data.get("sources", []):
            # 与 03 篇一致：语义相似度 < 0.5 的结果直接丢弃
            if float(item.get("similarity", 0)) < 0.5:
                continue
            out.append(RawEvidence(
                evidence_id=item["id"], channel="vane",
                source_id=self.registry.match(item["url"]),
                source_url=item.get("url"),
                source_title=item.get("title"),
                publish_date=item.get("publishedDate"),
                raw_text=item.get("content", ""),
                query_id=task.query_id,
                company_hint=task.company, dimension_hint=task.dimension,
                content_hash=sha1(item.get("content", "")),
            ))
        return out

    def _persist_snapshot(self, e: RawEvidence) -> RawEvidence:
        """原文快照落对象存储 —— 情报可溯源的最后防线"""
        if e.raw_text:
            key = f"snapshots/{e.fetched_at:%Y/%m/%d}/{e.evidence_id}.txt"
            self.storage.put(key, e.raw_text.encode("utf-8"))
            e.raw_snapshot_key = key
        return e
```

### 2.3 数据源注册中心（Source Registry）

**这是整个"稳定可靠信息源"的载体**。一源一档，字段分四组：

| 组 | 字段 | 说明 |
|---|---|---|
| 身份 | `source_id`, `name`, `domain`, `source_type`, `region` | `region ∈ {CN, EU, US, GLOBAL}` |
| 信任 | `base_credibility`(0-100), `tier`(official/industry/reference/low) | 基线来自 03 篇金字塔 |
| 能力 | `dimensions[]`, `connector`, `allow_general_search`, `rate_limit` | 决定它能不能被派发任务 |
| 动态 | `dynamic_score`, `hit_rate`, `freshness_days`, `conflict_rate`, `health`, `last_success_at` | 由自学习引擎回写 |

```python
# app/engines/source_registry.py
class SourceRegistry:
    def enabled_sources(self) -> set[str]:
        return {s.source_type for s in self._load() if s.enabled and s.health != "disabled"}

    def effective_credibility(self, source_id: str) -> float:
        """
        最终可信度 = 人工基线 * 动态修正
        人工基线永不被自动改写（安全边界，见 3.4）
        """
        s = self.get_by_id(source_id)
        return clamp(s.base_credibility * (0.6 + 0.4 * s.dynamic_score / 100), 0, 100)

    def mode_for(self, task: QueryTask) -> str:
        # 公告/年报/专利等权威源：fast 足够；新闻/舆情：quality 深度研究
        return "fast" if task.source_type in ("announcement", "annual_report") else "quality"

    def vane_sources_for(self, task: QueryTask) -> list[str]:
        base = ["web"]
        if task.source_type in ("industry_db", "patent"):
            base.append("academic")
        if task.source_type == "news":
            base.append("discussions")
        return base
```

**海外源分组（首批：欧盟 + 美国）**：

```yaml
# sources/policy-eu.yaml（结构示例，具体 URL 上线前校验）
region: EU
sources:
  - source_id: eu_eurlex_battery
    name: EUR-Lex 电池法规
    domain: eur-lex.europa.eu
    source_type: announcement
    tier: official
    base_credibility: 100
    connector: eu_lex
    params:
      celex: ["32023R1542"]          # EU 2023/1542 电池与废电池法规
    dimensions: [regulation, standard]
    lang: [en, zh]                    # 需翻译后入库，原文并保留

  - source_id: eu_echa
    name: ECHA 化学品/回收相关
    domain: echa.europa.eu
    source_type: announcement
    tier: official
    base_credibility: 95
    connector: null                   # 走 RSS + 通用搜索
```

```yaml
# sources/policy-us.yaml
region: US
sources:
  - source_id: us_federal_register
    name: Federal Register
    domain: federalregister.gov
    source_type: announcement
    tier: official
    base_credibility: 100
    connector: us_federal
    params:
      agencies: [DOE, EPA]
      terms: ["battery recycling", "lithium-ion", "critical minerals"]

  - source_id: us_doe_energy
    name: DOE Office of Energy Efficiency & Renewable Energy
    domain: energy.gov
    source_type: announcement
    tier: official
    base_credibility: 95
    connector: null
```

```yaml
# sources/policy-cn.yaml  —— 占位骨架，等你提供具体数据源后填充
region: CN
status: pending_user_input          # ← 待你补充 URL / 栏目
sources:
  # - source_id: cn_miit
  #   name: 工业和信息化部
  #   domain: miit.gov.cn
  #   ...
  # - source_id: cn_ndrc
  #   name: 国家发展改革委
  # - source_id: cn_mee
  #   name: 生态环境部
  # - source_id: cn_local_*
  #   各省工信厅 / 生态环境厅
  # - source_id: cn_std
  #   全国标准信息公共服务平台（GB/YS）
```

> 📌 **待你提供**：中国政策源的具体站点与栏目 URL。我拿到后会做三件事：① 逐个探测可达性与更新频率；② 判断能否结构化直采（能则写 Connector，不能则降级为定向搜索）；③ 回填 `base_credibility` 与 `region: CN`。

### 2.4 交叉验证模块（Cross Validator）

**与 03 篇 2.1 的五层决策树 1:1 实现**，不新增、不放宽。

**关键概念：断言聚类（Claim Clustering）**
同一事实会以不同措辞出现在多个来源，必须先聚成"断言"，再对断言打分，而不是对每条原文打分。

```python
# app/engines/validator.py
from pydantic import BaseModel

class Claim(BaseModel):
    claim_id: str
    company_id: str
    dimension: str
    field: str
    normalized_value: str          # 归一化后的值（"8万吨" → 80000.0 吨）
    value_type: str                # number | date | text | person | percent
    evidence_ids: list[str]
    quality_score: int = 0
    verification_level: str = "LOW"     # HIGH | MEDIUM | LOW
    flags: list[str] = []
    status: str = "pending"             # verified | pending_review | rejected


class CrossValidator:
    def __init__(self, registry, claude, cfg):
        self.registry, self.claude, self.cfg = registry, claude, cfg

    async def validate(self, claims: list[Claim], evidence: dict[str, RawEvidence]) -> list[Claim]:
        out = []
        for c in claims:
            c.quality_score = 50          # 基础分（与 03 篇一致）
            c.quality_score += self._layer1_authority(c, evidence)     # max +30
            c.quality_score += self._layer2_cross_source(c, evidence)  # max +20
            c.quality_score += await self._layer3_logic(c)             # max +15
            c.quality_score += self._layer4_freshness(c, evidence)     # max +10
            c.quality_score += self._layer5_traceability(c, evidence)  # max +5
            c.quality_score = clamp(c.quality_score, 0, 100)
            c.status = self._gate(c)      # 质量闸门，见 6.1
            out.append(c)
        return self._detect_conflicts(out, evidence)

    # ---------- 第一层：来源权威性 (max +30) ----------
    def _layer1_authority(self, c, evidence) -> int:
        creds = [self.registry.effective_credibility(evidence[eid].source_id)
                 for eid in c.evidence_ids if eid in evidence]
        if not creds:
            c.flags.append("no_source"); return 0
        best = max(creds)
        if best >= 95:  return 30
        if best >= 80:  return 20
        if best >= 70:  return 10
        return 0

    # ---------- 第二层：多源交叉验证 (max +20) ----------
    def _layer2_cross_source(self, c, evidence) -> int:
        # 关键：独立 = 不同域名且不同主办方，转载不算独立
        domains = {self._root_domain(evidence[eid].source_url)
                   for eid in c.evidence_ids if eid in evidence and evidence[eid].source_url}
        if len(domains) >= 3:
            c.verification_level = "HIGH";  return 20
        if len(domains) == 2:
            c.verification_level = "MEDIUM"; return 15
        c.verification_level = "LOW"
        c.flags.append("single_source")     # → 强制进人工队列
        return 0

    # ---------- 第三层：AI 逻辑合理性 (max +15) ----------
    async def _layer3_logic(self, c) -> int:
        """四项检查（与 03 篇一致）：一致性 / 业务逻辑 / 行业基准 / 时间序列"""
        result = await self.claude.check_logic(
            field=c.field, value=c.normalized_value, company_id=c.company_id,
            baseline=self._industry_baseline(c.field),
            history=self._history(c),
        )
        if result["passed"]:
            return 15
        c.flags.append("logic_inconsistency")
        c.flags += result.get("reasons", [])
        return -10

    # ---------- 第四层：时效性 (max +10) ----------
    def _layer4_freshness(self, c, evidence) -> int:
        dates = [evidence[eid].publish_date for eid in c.evidence_ids
                 if eid in evidence and evidence[eid].publish_date]
        if not dates: return 0
        days = (datetime.utcnow() - max(dates)).days
        if days <= 30:  return 10
        if days <= 90:  return 5
        if days <= 180: return 0
        c.flags.append("stale"); return -5

    # ---------- 第五层：溯源可追踪 (max +5) ----------
    def _layer5_traceability(self, c, evidence) -> int:
        evs = [evidence[eid] for eid in c.evidence_ids if eid in evidence]
        with_url = [e for e in evs if e.source_url]
        if evs and len(with_url) == len(evs):  return 5
        if with_url:                           return 2
        c.flags.append("no_source_url"); return -10

    def _detect_conflicts(self, claims, evidence) -> list[Claim]:
        """同一 (company, field, period) 出现互斥值 → 双方都标记冲突并进人工队列"""
        groups: dict[tuple, list[Claim]] = {}
        for c in claims:
            groups.setdefault((c.company_id, c.field), []).append(c)
        for key, items in groups.items():
            values = {i.normalized_value for i in items}
            if len(values) > 1:
                for i in items:
                    i.flags.append("data_conflict")
                    i.status = "pending_review"
                    i.quality_score -= 10
        return claims
```

**归一化规则（决定能不能比）**：

| 类型 | 规则 | 示例 |
|---|---|---|
| 数量 | 统一到基准单位 + 保留区间表达 | `8万吨` / `80,000吨` / `8万t` → `80000 t` |
| 产能区间 | 保留下限/上限 | `5-8万吨` → `[50000, 80000] t` |
| 时间 | 统一 ISO 日期；`2024年Q1` → `2024-Q1` | — |
| 百分比 | 小数归一 | `85%` → `0.85` |
| 人物 | 姓名 + 职务 + 任期元组 | — |
| 实体 | 走别名库（见 2.5） | `荆门格林美` → `gemm` |

### 2.5 实体消解与去重（Entity Resolver）

**三层去重**，成本从低到高，前层命中就不进后层：

```
第 1 层：精确去重 ── content_hash 完全一致 → 丢弃（同一篇文章被多源转载）
第 2 层：近重复 ──── SimHash(64bit) 汉明距离 ≤ 3 → 合并，保留权威源那条
第 3 层：语义去重 ── 向量余弦 ≥ 0.92 → 合并为同一"证据簇"，全部 evidence_id 保留
                     （用于交叉验证的"独立来源计数"，而不是真删）
```

```python
# app/engines/resolver.py
class EntityResolver:
    def __init__(self, aliases: dict[str, str], embedder, simhash_bits=64):
        self.aliases = aliases          # 别名 → 标准 id
        self.embedder = embedder

    def resolve_company(self, text: str, hints: list[str]) -> str | None:
        """先别名精确匹配，再用向量兜底"""
        for hint in hints:
            if hint in self.aliases:
                return self.aliases[hint]
        for alias, cid in self.aliases.items():
            if alias in text:
                return cid
        best, score = None, 0.0
        text_vec = self.embedder.encode(text[:512])
        for alias, cid in self.aliases.items():
            s = cosine(text_vec, self.embedder.encode(alias))
            if s > score:
                best, score = cid, s
        return best if score >= 0.85 else None      # 低于阈值宁可不判，交人工
```

**别名库来源**：① 你手工确认的（最高优先级，锁定）；② 年报/公告中的全称与简称；③ 子公司名（从工商/公告抽取）；④ Claude 建议（**需你确认后才写入**，避免误合并）。

### 2.6 结构化抽取模块（Extractor）

把 `RawEvidence.raw_text` 抽成 `Claim`，两级策略：

```python
# app/engines/extractor.py
class Extractor:
    """
    策略 A：规则/模板抽取 —— 年报表格、环评公示、公告标题，结构化程度高，准确率 >95%
    策略 B：Claude 抽取 —— 新闻、评论、长文本
    策略 A 命中就不走 B（省 token 且更准）
    """

    async def extract(self, ev: RawEvidence, spec: DimensionSpec) -> list[Claim]:
        claims = self._rule_based(ev, spec)
        if not claims and self._worth_llm(ev):
            claims = await self._llm_based(ev, spec)
        return [self._normalize(c) for c in claims]

    def _worth_llm(self, ev: RawEvidence) -> bool:
        # 太短的片段没有抽取价值；纯导航页跳过
        return len(ev.raw_text) > 200 and not self._is_nav_page(ev.raw_text)

    async def _llm_based(self, ev, spec):
        """
        强制 JSON 输出 + pydantic 校验，校验失败自动重试一次
        每条 claim 必须带 source_quote（原文片段），便于人工审核时一眼定位
        """
        schema_hint = {f: "string|number|null" for f in spec.fields}
        result = await self.claude.extract_fields(
            text=ev.raw_text, fields=schema_hint,
            company=ev.company_hint, dimension=spec.key,
            require_quote=True,
        )
        return [Claim(..., field=f, normalized_value=v["value"],
                      source_quote=v["quote"]) for f, v in result.items() if v.get("value")]
```

**必带 `source_quote`**：人工审核时可以直接跳转到原文对应位置，这是"数据可验证"的底线。

### 2.7 Claude 深度分析适配层（薄层）

后端**只负责**：异步任务编排、结构化出入参、结果落库、失败重试、成本统计。
**Prompt 模板、分析框架、报告结构全部在 [08_Claude集成和深度分析.md](08_Claude集成和深度分析.md)**。

```python
# app/engines/claude_adapter.py
class ClaudeAdapter:
    """所有方法都返回已校验的 Pydantic 对象；不做任何业务判断"""

    async def check_logic(self, **kw) -> dict: ...
    async def extract_fields(self, **kw) -> dict: ...
    async def suggest_queries(self, goal, company, dims, period) -> list[QueryTask]: ...
    async def suggest_aliases(self, company, candidates) -> list[str]: ...
    async def analyze_company(self, company_id, claims, dimension) -> dict: ...
    async def generate_report(self, template_id, company_ids, period) -> dict: ...
```

**成本与稳定性约束**：

| 约束 | 值 | 原因 |
|---|---|---|
| 单次 `extract_fields` 输入上限 | 8k tokens | 超长文本先切片，避免上下文稀释 |
| 逻辑检查批量 | 20 claims / 次 | 兼顾成本与准确率 |
| 失败重试 | 3 次，指数退避 | 429/5xx |
| JSON 校验失败 | 重试 1 次后丢弃并记 `llm_parse_error` | 不污染数据 |
| 每日 token 预算 | 可配置，超额告警到钉钉 | 防失控 |

### 2.8 人工审核服务（单人工作台）

**只有你一个人审核**，所以设计目标是**吞吐**而不是协同：

```python
# app/api/v1/review.py
@router.get("/review/queue")
def queue(priority: str | None = None, dimension: str | None = None,
          limit: int = 50, db=Depends(get_db)):
    """
    排序策略（单人效率优先）：
      1. 高优先级待审（<60分 / 冲突 / 关键决策相关）
      2. 同企业同维度聚在一起（切上下文成本最低）
      3. 新旧混排比例 7:3（避免一直在读老数据）
    """
```

**三个提效机制**：

| 机制 | 做法 |
|---|---|
| 批量操作 | 支持多选后 `批量通过/批量拒绝/批量降级`，一次请求处理 N 条 |
| 键盘流 | `j/k` 上下、`a` 通过、`r` 拒绝、`o` 打开原文、`u` 撤销（前端实现见 05 篇） |
| 差异视图 | 同一 claim 的多来源并排展示，冲突项高亮，一眼看出分歧点 |

**状态机（简化版，单人够用）**：

```
pending ──通过──→ verified ──┐
   │                          ├──→ archived（被更新值取代）
   ├──拒绝──→ rejected ───────┘
   ├──返工──→ pending (加 note)
   └──待观察──→ watchful ──到期──→ pending
```

**审核动作必须留痕**：`review_logs` 记录 `claim_id / action / before / after / note / created_at`，用于回溯"我当初为什么这么判"。

### 2.9 对话检索代理（保留 Vane 入口）

**你的要求：必须保留对话式检索入口**。做法是**双通道共用一套后处理管线**：

```
┌── Web 对话检索（你手敲）──┐
│  POST /api/v1/chat/search  │
└───────────┬───────────────┘
            │
┌── 后台批量采集（Celery）──┐
│  POST /api/v1/collect      │
└───────────┬───────────────┘
            ▼
   ┌─────────────────────────────────────┐
   │  同一条管线：清洗 → 抽取 → 验证 → 入库 │
   └─────────────────────────────────────┘
```

```python
# app/api/v1/chat.py
@router.post("/chat/search")
async def chat_search(req: ChatSearchReq, user=Depends(auth)):
    """
    1) 透传 Vane 拿原始结果 → 立刻流式返回（你要的是"能用"）
    2) 后台异步走验证管线（你要的是"准"）
    3) 返回体里带 task_id，前端可轮询验证进度
    """
    raw = await vane.search(req.dict())                     # 秒级返回
    task = validate_and_ingest.delay(raw, req.query)         # 后台验证
    return {"raw": raw, "validation_task_id": task.id,
            "note": "原始结果未经交叉验证，仅作检索用途；已验证版本见 /evidence"}
```

**关键收益**：你日常顺手的一次搜索，**也会自动变成正式数据**，不用重复劳动；同时前端能明确区分「原始检索结果」和「已验证数据」两个标签页，避免你担心的"错误数据被当成结论展示"。

---

## 第三部分：数据源自学习与自纠错

> 这是你需求里最核心的一条：**"自己能够根据后端交付的数据来发现数据源规律，自己纠错"**。
> 设计原则：**机器负责发现和降权，人负责删除和新增**——自动动作永远可逆。

### 3.1 源效果评分模型（Source Score Model）

对每个数据源，按四个维度滚动统计（默认 90 天窗口）：

| 指标 | 计算 | 含义 |
|---|---|---|
| `hit_rate` | 该源产生的 claim 中，最终 `verified` 的比例 | 越准越高 |
| `unique_rate` | 该源独有（其他源没有）的 claim 比例 | 越高越不可替代 |
| `conflict_rate` | 该源参与冲突的比例 | 越高越可疑 |
| `latency_days` | 事实发生 → 该源报道的平均天数 | 越短越及时 |
| `liveness` | 近 30 天成功请求比例 | 判断源是否失效/改版 |

```python
# app/engines/source_score.py
def dynamic_score(m: SourceMetrics) -> float:
    """
    dynamic_score ∈ [0, 100]
    权重设计理由：
      - hit_rate 权重最高（0.40）：准确是第一位的，宁缺毋滥
      - unique_rate 0.25：不可替代的源要保护
      - freshness 0.20：情报的时效价值
      - liveness 0.15：基础设施健康度
    """
    freshness = clamp(100 - m.latency_days * 6, 0, 100)
    score = (0.40 * m.hit_rate * 100
             + 0.25 * m.unique_rate * 100
             + 0.20 * freshness
             + 0.15 * m.liveness * 100)
    return clamp(score - 0.30 * m.conflict_rate * 100 + 50 * 0.30, 0, 100)


def effective_credibility(base_credibility: float, dynamic: float) -> float:
    """
    人工基线永远是锚：动态分只能在 ±40% 内浮动
      base=100 (官方源) → 动态区间 [60, 100]
      base=60  (低可信) → 动态区间 [36, 60]
    杜绝"某个源刷量把自己刷成权威"的情况
    """
    return clamp(base_credibility * (0.6 + 0.4 * dynamic / 100), 0, 100)
```

### 3.2 规律发现引擎（Pattern Miner）

**按周运行**，产出「规律报告」推送到钉钉，内容包括：

```python
# app/engines/pattern_miner.py
class PatternMiner:
    """
    发现 5 类规律（每类都必须可解释、可复核）
    """

    def find_patterns(self, window_days: int = 90) -> list[Pattern]:
        return [
            *self._p1_emerging_sources(),      # 新兴高效源
            *self._p2_declining_sources(),     # 退化源
            *self._p3_dimension_coverage(),    # 维度覆盖缺口
            *self._p4_time_patterns(),         # 发布时间规律
            *self._p5_company_patterns(),      # 企业专属规律
        ]

    def _p1_emerging_sources(self) -> list[Pattern]:
        """新兴源：首次出现 < 60 天，且 hit_rate ≥ 0.8，且贡献 ≥ 3 条 verified"""
        # → 建议：提升其权重 / 为其编写专用 Connector

    def _p2_declining_sources(self) -> list[Pattern]:
        """退化源：近 30 天 liveness < 0.5，或 conflict_rate > 0.3"""
        # → 建议：降权 / 检查是否改版 / 是否被墙

    def _p3_dimension_coverage(self) -> list[Pattern]:
        """
        维度覆盖缺口 —— 最贴合你的痛点：
          - 某企业某维度的 verified claim 数量 < 阈值 → 该维度信息源不足
          - 某维度 avg_quality_score < 70 → 该维度源质量不够，需补充权威源
        例："邦普的『废料量及来源』近 90 天仅 2 条 verified，
             且 avg_quality=64，建议补充：湖南省生态环境厅环评、宁德时代公告"
        """

    def _p4_time_patterns(self) -> list[Pattern]:
        """时间规律：年报 4 月集中、环评公示多在周二/周四、
        欧盟法规多在 Q4 —— 据此调整采集周期，减少无效抓取"""

    def _p5_company_patterns(self) -> list[Pattern]:
        """
        企业专属规律：
          - 格林美公告集中在巨潮 → 提高巨潮该股票代码采集频率
          - 邦普无独立公告，只有母公司穿透 → 强化宁德时代公告监控
        """
```

**规律报告示例（钉钉卡片）**：

```
📊 数据源规律周报（2026-09-01 ~ 2026-09-07）

🟢 新兴高效源
  · cninfo_gemm_announcement  命中率 96%  贡献 41 条已核实
    → 建议：从 weekly 提升到 daily（已自动执行）

🔴 退化源
  · xxx_news_site  存活率 32%（疑似改版）
    → 已自动降权 30%；请确认是否仍可用

⚠️ 维度覆盖缺口
  · 邦普 / 废料量及来源：仅 2 条 verified（阈值 ≥5）
    建议补充源：湖南省生态环境厅、宁德时代公告
  · 格林美 / 人力资源：avg_quality 68（阈值 ≥70）

⏰ 时间规律
  · 发现环评公示集中于周二、周四发布
    → 建议：环评 Channel 采集日调整为周二/四（待你确认）
```

### 3.3 自动动作矩阵（哪些自动、哪些必须你点）

| 动作 | 自动？ | 触发条件 | 可逆性 |
|---|---|---|---|
| 提高采集频率 | ✅ 自动 | `hit_rate ≥ 0.9` 且 `liveness ≥ 0.9` | 可逆，记录在案 |
| 降低采集频率 | ✅ 自动 | 连续 4 周 `hit_rate < 0.4` | 可逆 |
| 降权（动态分 -30%） | ✅ 自动 | `conflict_rate > 0.3` 或 `liveness < 0.5` | 可逆 |
| 标记 `health=degraded` | ✅ 自动 | 连续 5 次请求失败 | 可逆 |
| **禁用某源** | ❌ 需你确认 | `liveness=0` 持续 14 天 | 手动恢复 |
| **删除某源** | ❌ 需你确认 | — | 软删除，可恢复 |
| **新增候选源** | ⚠️ 自动发现 + 你确认 | 新兴源规律触发 | 未确认前仅"观察"状态 |
| **修改 base_credibility** | ❌ 禁止自动 | — | **只能你手工改** |
| **合并企业实体** | ❌ 需你确认 | 向量相似 ≥ 0.85 | 可撤销 |

### 3.4 人工干预与安全边界

**三条铁律**：

1. **人工基线不可被自动改写**——`base_credibility` 是锚点，动态分只能在 ±40% 内浮动。
2. **自动动作全部留痕且可回滚**——`source_actions` 表记录 `action / reason / before / after / reversible`。
3. **自动永不删除数据**——只做降权、标记、隐藏；删除必须你手工操作。

**你的手工干预入口**（API + 前端）：

```http
# 锁定某源为"永不降权"（例如你确认的权威源）
POST /api/v1/sources/{source_id}/lock
{ "pinned_credibility": 100, "reason": "工信部官网，长期权威" }

# 手工新增源
POST /api/v1/sources
{ "name": "某省生态环境厅", "domain": "...", "source_type": "eia",
  "base_credibility": 95, "region": "CN", "dimensions": ["feedstock"],
  "connector": null, "allow_general_search": true }

# 手工投喂一条已知准确信息（跳过部分验证）
POST /api/v1/evidence/manual
{ "company_id": "gemm", "dimension": "operation", "field": "designed_capacity",
  "value": "80000 t", "source_url": "...", "note": "官方公告确认",
  "trust_level": 100 }
```

---

## 第四部分：任务调度与执行

### 4.1 任务类型

| 任务 | 队列 | 触发 | 典型耗时 |
|---|---|---|---|
| `collect.company.daily` | collect | 每日 02:00 | 5-15 min |
| `collect.company.full` | collect | 手动 / 每周日 03:00 | 30-90 min |
| `collect.policy.weekly` | collect | 每周一 08:00 | 10-30 min |
| `collect.policy.overseas` | collect | 每周一 09:00（EU/US） | 10-20 min |
| `analyze.company` | analyze | 采集完成后自动串联 | 3-10 min |
| `analyze.report` | analyze | 手动 / 每月 1 日 | 5-15 min |
| `maintain.source_score` | maintain | 每日 04:00 | <1 min |
| `maintain.pattern_mining` | maintain | 每周一 07:00 | 1-3 min |
| `maintain.dedup_gc` | maintain | 每日 05:00 | 1-5 min |

**调度计划**（对应你的"政策每周 / 企业希望每天"）：

```python
# app/tasks/celery_app.py
app.conf.beat_schedule = {
    # 公告类变动频繁 → 每日（你的 5 维度里 strategy 最活跃）
    "company-daily": {
        "task": "tasks.collect_tasks.company_daily",
        "schedule": crontab(hour=2, minute=0),
        "kwargs": {"dimensions": ["strategy", "operation"], "mode": "fast"},
    },
    # 全维度深挖 → 每周（质量模式，覆盖更广）
    "company-full-weekly": {
        "task": "tasks.collect_tasks.company_full",
        "schedule": crontab(day_of_week=0, hour=3, minute=0),
        "kwargs": {"mode": "quality"},
    },
    # 政策：每周（你指定）
    "policy-cn-weekly": {
        "task": "tasks.collect_tasks.policy_weekly",
        "schedule": crontab(day_of_week=1, hour=8, minute=0),
        "kwargs": {"region": "CN"},
    },
    "policy-eu-us-weekly": {
        "task": "tasks.collect_tasks.policy_weekly",
        "schedule": crontab(day_of_week=1, hour=9, minute=0),
        "kwargs": {"region": ["EU", "US"]},
    },
    "source-score-daily":  {"task": "tasks.maintain_tasks.source_score",  "schedule": crontab(hour=4)},
    "pattern-mining-weekly": {"task": "tasks.maintain_tasks.pattern_mining", "schedule": crontab(day_of_week=1, hour=7)},
}
```

### 4.2 并发、限流与重试

```python
# 单源限流：绝不因为"我要数据"把对方站点打挂
RATE_LIMITS = {
    "cninfo":            {"rps": 0.5, "burst": 2},   # 上市公司数据源，保守
    "eia":               {"rps": 0.3, "burst": 1},   # 政府站点，务必克制
    "patent":            {"rps": 0.5, "burst": 2},
    "eur_lex":           {"rps": 2.0, "burst": 4},
    "us_federal":        {"rps": 2.0, "burst": 4},
    "vane":              {"rps": 1.0, "burst": 3},   # 本地，受 SearxNG 上游限制
}

# 重试策略：可重试的错误才重试
RETRY_POLICY = {
    "max_retries": 3,
    "backoff": "exponential",       # 2s → 8s → 32s，带抖动
    "retry_on": [429, 500, 502, 503, 504, "TimeoutError", "ConnectError"],
    "no_retry_on": [400, 401, 403, 404],   # 403 直接告警（可能被墙）
}
```

### 4.3 幂等与断点续采

| 问题 | 方案 |
|---|---|
| 重复采集同一篇文章 | `evidence.content_hash` 唯一索引，`INSERT IGNORE` |
| 任务重跑导致重复 claim | `claims.fingerprint = sha1(company\|field\|period\|normalized_value)` 唯一 |
| 采集中途失败 | 任务分片：每 `(company, dimension, source)` 一个子任务独立落库，失败只重跑该片 |
| 慢源拖垮整批 | 单子任务硬超时（默认 300s），超时标记 `timeout` 继续下一片 |

```python
# app/tasks/collect_tasks.py
@app.task(bind=True, max_retries=3, acks_late=True)
def collect_slice(self, company_id: str, dimension: str, source_type: str, period: str):
    """分片粒度：(企业, 维度, 源类别) —— 最小可重试单元"""
    try:
        tasks = decomposer.decompose(...)
        for t in tasks:
            run_pipeline(t)          # 采集 → 抽取 → 验证 → 入库
        db.commit()
    except SoftTimeLimitExceeded:
        db.rollback()
        record_partial(company_id, dimension, source_type, "timeout")
        raise self.retry(countdown=60)
```

---

## 第五部分：API 接口设计

### 5.1 端点总览

| 方法 | 路径 | 用途 | 对应前端 |
|---|---|---|---|
| `POST` | `/api/v1/collect` | 创建采集任务 | 05 §2.2 采集页 |
| `GET` | `/api/v1/collect/{task_id}` | 任务状态与进度 | 05 §2.2 |
| `GET` | `/api/v1/collect/{task_id}/stream` | SSE 实时进度 | 05 §2.2 |
| `DELETE` | `/api/v1/collect/{task_id}` | 取消任务 | 05 §2.2 |
| `POST` | `/api/v1/chat/search` | **对话检索代理（保留入口）** | 05 §2.1 |
| `GET` | `/api/v1/companies` | 企业列表（含质量摘要） | 05 §2.4 |
| `GET` | `/api/v1/companies/{id}` | 企业档案（5 维度聚合） | 05 §2.4 |
| `GET` | `/api/v1/companies/{id}/timeline` | 企业事件时间线 | 05 §2.4 |
| `GET` | `/api/v1/evidence` | 证据列表（多条件筛选） | 05 §2.3 |
| `GET` | `/api/v1/evidence/{id}` | 证据详情 + 原文快照 + 多源对照 | 05 §2.3 |
| `GET` | `/api/v1/claims/{id}/sources` | 断言的来源对比（跨源） | 05 §2.3 |
| `GET` | `/api/v1/review/queue` | 待审队列 | 05 §2.3 |
| `POST` | `/api/v1/review/{claim_id}` | 单条审核（通过/拒绝/返工/待观察） | 05 §2.3 |
| `POST` | `/api/v1/review/batch` | 批量审核 | 05 §2.3 |
| `GET` | `/api/v1/policies` | 政策库（区域/类型/时间筛选） | 05 §2.1 |
| `GET` | `/api/v1/policies/timeline` | 政策时间线 | 05 §2.1 |
| `GET` | `/api/v1/sources` | 源列表（含动态分与健康度） | 05 §2.5 |
| `POST` | `/api/v1/sources` | 手工新增源 | 05 §2.5 |
| `POST` | `/api/v1/sources/{id}/lock` | 锁定基线 | 05 §2.5 |
| `GET` | `/api/v1/analytics/source-health` | 源健康度报表 | 05 §2.5 |
| `GET` | `/api/v1/analytics/patterns` | 规律发现报告 | 05 §2.5 |
| `POST` | `/api/v1/analysis/company/{id}` | 触发 Claude 深度分析 | 05 §2.4 |
| `POST` | `/api/v1/reports` | 生成报告 | 05 §2.1 |
| `GET` | `/api/v1/reports/{id}` | 获取报告 | 05 §2.1 |

### 5.2 关键接口示例

**创建采集任务**：

```http
POST /api/v1/collect
Authorization: Bearer <token>
Content-Type: application/json

{
  "companies": ["gemm", "brunp"],
  "dimensions": ["hr", "feedstock", "operation", "strategy", "technology"],
  "period": "2024-2026",
  "mode": "quality",
  "source_scope": "all",
  "priority": "high",
  "note": "第三轮深挖，重点补邦普废料来源"
}
```

```json
{
  "task_id": "tsk_7f3a91c2",
  "status": "queued",
  "estimated_minutes": 45,
  "slices": 30,
  "created_at": "2026-09-10T19:20:00+08:00"
}
```

**待审队列（单人工作台优化）**：

```http
GET /api/v1/review/queue?priority=high&group_by=company_dimension&limit=50
```

```json
{
  "total": 137,
  "groups": [
    {
      "group_key": "brunp/feedstock",
      "count": 12,
      "items": [
        {
          "claim_id": "clm_9a12",
          "field": "total_recycled_volume",
          "value": "12万吨",
          "quality_score": 58,
          "flags": ["single_source", "data_conflict"],
          "verification_level": "LOW",
          "conflict_with": [
            {"claim_id": "clm_9a13", "value": "10万吨", "quality_score": 74,
             "source": "湖南邦普环评公示", "url": "..."}
          ],
          "sources": [
            {"name": "某行业媒体", "tier": "reference", "url": "...", "publish_date": "2026-08-12"},
            {"name": "知乎讨论", "tier": "low", "url": "...", "publish_date": "2025-11-03"}
          ],
          "suggested_action": "reject",
          "suggested_reason": "仅参考级与低可信源，与环评公示数值冲突，建议驳回或转待观察"
        }
      ]
    }
  ]
}
```

**企业档案（5 维度聚合，只返回已验证数据）**：

```http
GET /api/v1/companies/gemm?dimensions=all&only_verified=true
```

```json
{
  "company": {"id": "gemm", "name": "格林美", "stock_code": "002340.SZ"},
  "data_policy": "only_verified",
  "dimensions": {
    "operation": {
      "designed_capacity": {
        "value": "80000 t",
        "quality_score": 92,
        "verification_level": "HIGH",
        "confirmed_by": 3,
        "as_of": "2026-04-20",
        "sources": ["2025年年度报告", "巨潮公告", "北极星电池网"],
        "trend": [
          {"period": "2024", "value": "60000 t", "quality_score": 88},
          {"period": "2025", "value": "80000 t", "quality_score": 92}
        ]
      }
    }
  },
  "coverage": {
    "verified_claims": 214,
    "pending_review": 37,
    "gaps": ["feedstock.regional_distribution", "technology.unit_cost"]
  }
}
```

### 5.3 异步任务与进度推送

```javascript
// SSE 进度推送（前端 05 §2.2 使用）
const es = new EventSource(`/api/v1/collect/${taskId}/stream`);
es.onmessage = (e) => {
  const p = JSON.parse(e.data);
  // { stage, current, total, company, dimension, found, verified, pending, errors }
};
```

```python
# app/tasks/collect_tasks.py
def run_pipeline(t: QueryTask):
    notify_progress(t, stage="collecting")
    evs = collector.collect(t)
    notify_progress(t, stage="extracting", found=len(evs))

    claims = extractor.extract_batch(evs, DIMENSIONS[t.dimension])
    claims = resolver.dedupe(claims)
    claims = validator.validate_sync(claims, {e.evidence_id: e for e in evs})

    verified = [c for c in claims if c.status == "verified"]
    pending  = [c for c in claims if c.status == "pending_review"]
    persist(verified); enqueue_review(pending)

    notify_progress(t, stage="done", verified=len(verified), pending=len(pending))
    if pending and is_high_priority(t):
        alerts.push_review_needed(pending)      # 钉钉
```

### 5.4 鉴权、限流与错误码

```python
# 单人使用，用静态 Token 即可，不必上 OAuth
API_TOKEN = settings.API_TOKEN          # .env 配置，Nginx 层也校验一次
RATE_LIMIT = "600/minute"               # 前端正常使用打不到这个量
```

| 错误码 | 含义 | 处理 |
|---|---|---|
| `40001` | 参数校验失败 | 返回 pydantic 明细 |
| `40101` | Token 无效 | 401 |
| `40301` | 源被禁用 / 无权限 | 403，附原因 |
| `40401` | 资源不存在 | 404 |
| `42901` | 上游限流（源站） | 自动重试，前端显示"源站限流中" |
| `50001` | 采集失败 | 返回 task_id 便于排查 |
| `50002` | 抽取失败（LLM 解析错误） | 已重试 1 次，转人工 |
| `50003` | 数据库/存储异常 | 告警到钉钉 |

---

## 第六部分：质量保障与可观测性

### 6.1 质量闸门（Quality Gate）

**这是"数据最重要"的技术兑现点**：未过闸的数据**物理上进不了正式表**。

```
                    ┌─────────────────────┐
   验证完成 ────────▶│   Quality Gate      │
                    └──────────┬──────────┘
                               │
       ┌───────────────────────┼───────────────────────┐
       ▼                       ▼                       ▼
  score ≥ 80             60 ≤ score < 80          score < 60
  verification=HIGH/MED
       │                       │                       │
       ▼                       ▼                       ▼
  claims(verified)      review_queue(pending)    review_queue(high_priority)
  ↑ 前端默认可见         ↑ 前端默认隐藏           ↑ 前端默认隐藏
  ↑ 可用于分析/报告      ↑ 需你审核               ↑ 需你审核（24h 内）
```

```python
# app/core/quality_gate.py
def gate(claim: Claim) -> str:
    if "no_source_url" in claim.flags or "no_source" in claim.flags:
        return "pending_review"          # 无源 → 一律人工，无论多少分
    if claim.quality_score >= 80:
        return "verified"
    if claim.quality_score >= 60:
        return "pending_review"
    return "pending_review"              # 低分统一进队列，由优先级排序区分
```

**三条硬规则**（不可配置绕过）：

1. 无 URL / 无来源 → 永不 `verified`。
2. 存在 `data_conflict` → 永不 `verified`（必须你裁决）。
3. 单一来源 + 非官方源 → 永不 `verified`（对应你的"交叉验证"要求）。

### 6.2 钉钉告警

```python
# app/core/alerts.py
class DingTalk:
    """
    用自定义机器人 Webhook + 加签。
    分级：critical(立即@你) / warning(聚合推送) / info(日报汇总)
    """

    def push_review_needed(self, pending: list[Claim]):
        # warning 级：聚合，避免刷屏
        ...

    def push_critical(self, msg: str, detail: str):
        # critical：源站 403（可能被墙）、连续 5 次采集失败、
        #           月度 token 预算超 80%、出现高价值冲突数据
        ...

    def push_source_pattern_report(self, patterns: list[Pattern]):
        # 每周一 07:30，规律发现报告卡片
        ...

    def push_daily_digest(self, stats: dict):
        # 每日 09:00：新增/已验证/待审/失败 统计
        ...
```

**告警清单**：

| 级别 | 事件 | 渠道 |
|---|---|---|
| 🔴 critical | 源站返回 403/404 连续 5 次 | 钉钉 @你 |
| 🔴 critical | 采集任务连续 3 次失败 | 钉钉 @你 |
| 🔴 critical | Claude token 月预算 > 80% | 钉钉 @你 |
| 🟠 warning | 新增待审 > 50 条 | 钉钉卡片 |
| 🟠 warning | 发现 `data_conflict` 且双方均 ≥75 分 | 钉钉卡片 |
| 🟠 warning | 某维度 90 天 verified < 5 条 | 钉钉卡片 |
| 🔵 info | 每日采集摘要 | 钉钉卡片（09:00） |
| 🔵 info | 数据源规律周报 | 钉钉卡片（周一 07:30） |

### 6.3 日志与埋点

**结构化 JSON 日志 + 全链路 trace**：

```python
# app/core/logging.py
logger.info("evidence_collected", extra={
    "trace_id": ctx.trace_id,
    "task_id": ctx.task_id,
    "company": task.company,
    "dimension": task.dimension,
    "source_id": ev.source_id,
    "query": task.query,
    "found": len(evs),
    "elapsed_ms": elapsed,
})
```

**必须记录的关键指标**（供 09 篇监控看板使用）：

| 指标 | 用途 |
|---|---|
| `collect.success_rate` by source | 发现退化源 |
| `claim.verified_rate` by source/dimension | 源质量评估 |
| `claim.avg_quality_score` by dimension | 维度覆盖质量 |
| `review.pending_count` / `review.avg_age_hours` | 审核积压 |
| `llm.tokens_used` / `llm.cost_estimate` | 成本控制 |
| `dedup.merge_rate` | 去重有效性（过高说明源同质化） |
| `pipeline.p95_latency_ms` | 性能 |

### 6.4 测试策略

| 层 | 工具 | 重点用例 |
|---|---|---|
| 单元测试 | pytest | 归一化规则（`8万吨`↔`80000 t`）、质量评分五层、闸门规则、SimHash 去重 |
| 契约测试 | pytest + respx | Vane API / Claude API 的 mock，防止上游字段变更 |
| 连接器测试 | pytest + vcr.py | 录制真实响应，避免测试时打真站 |
| 端到端 | pytest + 测试库 | 一条完整情报：采集→抽取→验证→入库→审核 |
| 回归测试 | 金标数据集 | 手工标注 50 条 claim，验证评分与闸门结果稳定（**改评分公式必跑**） |

**金标数据集**是这套系统的"质量安全带"：任何改动验证逻辑的提交，都必须保持金标集上的判定结果不劣化。

---

## 第七部分：三阶段落地路线

### 7.1 第一阶段（核心功能，1-2 周）— 对应你选的 ABC

| 编号 | 交付 | 验收标准 |
|---|---|---|
| A | **核心采集**：需求拆解 + 三通道采集 + 漏斗入库 | 格林美、邦普 5 维度可跑通，产出 ≥100 条 claim |
| B | **基础监测**：政策周更（CN 为主，EU/US 骨架）+ 去重去噪 | 每周一自动出政策清单，重复率 <5% |
| C | **简单 Dashboard**：企业信息表格 + 政策更新列表 + 待审队列 | 浏览器打开能看到已验证数据，未验证数据默认隐藏 |

**第一周**：项目骨架 + docker-compose + MySQL Schema（06 篇）+ 数据源注册中心 + 拆解器
**第二周**：Vane 对接 + 2 个定向 Connector（巨潮、环评）+ 交叉验证五层 + 质量闸门 + 钉钉告警 + 最小 Dashboard 接口

> ⚠️ 第一阶段**不做**：Claude 深度分析报告、源自学习、前端美化。先把"数据能进、能验、能看"闭环。

### 7.2 第二阶段（增强，1 个月）— 对应你选的 ABC

| 编号 | 交付 | 验收标准 |
|---|---|---|
| A | **Claude 集成**：逻辑检查 + 字段抽取 + 竞对报告生成 | 一键出格林美 vs 邦普对比报告，每条结论带来源 |
| B | **高级数据处理**：多源交叉验证完善 + 实体消解 + 关联图谱 | 冲突数据自动识别并提示；同一事实自动聚合 |
| C | **看板升级 + 源自学习**：企业对标、政策时间线、源健康度、规律周报 | 钉钉每周收到源规律报告 |

### 7.3 第三阶段（全球覆盖，后续）— 对应你选的 AB

| 编号 | 交付 |
|---|---|
| A | **国际政策监测**：EU（EUR-Lex 全量）、US（Federal Register / DOE / EPA）+ 更多辖区 |
| B | **多语言支持**：英文原文 + 中文摘要双写入库；跨语言实体对齐 |

### 7.4 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 定向源改版/反爬 | 采集中断 | 每源独立 Connector + `liveness` 监控 + 403 立即告警；关键源保留 Playwright 兜底 |
| LLM 抽取幻觉 | 脏数据入库 | 强制 `source_quote` + 质量闸门 + 无源永不 verified |
| 企业别名误合并 | 数据串企业 | 向量阈值 0.85 + 人工确认制 + 合并可撤销 |
| Claude 成本失控 | 费用超支 | 日/月预算上限 + 规则优先 + 批量检查 |
| 政策源覆盖不全 | 漏政策 | 维度覆盖缺口规律自动提示（3.2 · P3） |
| 单人审核积压 | 数据不流通 | 批量操作 + 分组排序 + 自动降级"待观察" |

---

## 附录：环境变量清单（`.env`）

```bash
# ---- 运行环境 ----
ENV=production
API_TOKEN=change_me_long_random_token
TZ=Asia/Shanghai

# ---- MySQL ----
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_USER=battery
MYSQL_PASSWORD=change_me
MYSQL_DB=battery_osint
MYSQL_ROOT_PASSWORD=change_me_root

# ---- Redis ----
REDIS_URL=redis://:change_me@redis:6379/0

# ---- 对象存储（云服务器自建 MinIO；换云 OSS 只需改这三项）----
STORAGE_BACKEND=minio                # minio | oss | s3
MINIO_ENDPOINT=minio:9000
MINIO_USER=minioadmin
MINIO_PASSWORD=change_me
MINIO_BUCKET=battery-osint
MINIO_SECURE=false

# ---- Vane ----
VANE_URL=http://vane:3000
VANE_TIMEOUT=120

# ---- Claude ----
ANTHROPIC_API_KEY=sk-ant-...
CLAUDE_MODEL=claude-sonnet-4-6
CLAUDE_MAX_TOKENS_DAILY=2000000
CLAUDE_MAX_TOKENS_MONTHLY=40000000

# ---- 质量闸门（三条硬规则不可配置）----
QUALITY_VERIFIED_THRESHOLD=80
QUALITY_REVIEW_THRESHOLD=60
SIMILARITY_FILTER_THRESHOLD=0.5       # 与 03 篇一致
DEDUP_SEMANTIC_THRESHOLD=0.92

# ---- 采集策略 ----
COMPANY_DAILY_DIMENSIONS=strategy,operation
COMPANY_WEEKLY_MODE=quality
POLICY_CN_SCHEDULE=weekly
POLICY_OVERSEAS_REGIONS=EU,US

# ---- 钉钉 ----
DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=...
DINGTALK_SECRET=SEC...
DINGTALK_AT_MOBILE=138xxxxxxxx
```

---

**下一步推荐阅读**：[05_二次开发方案-前端.md](05_二次开发方案-前端.md)
